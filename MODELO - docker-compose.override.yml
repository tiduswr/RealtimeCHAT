version: '3.7'

services:
  database:
    environment:
      - MYSQL_DATABASE=
      - MYSQL_USER=
      - MYSQL_PASSWORD=
      - MYSQL_ROOT_PASSWORD=

  backend:
    environment:
      - DB_NAME=
      - DB_USERNAME=
      - DB_PASSWORD=
      - JWT_SECRET=